# Production dependencies only - matches [project].dependencies in pyproject.toml.
# Pin with: make lock -> generates requirements.txt
# Do not install from this file in CI or Docker - use requirements.txt instead.
fastapi>=0.121
starlette>=0.46.0,<2.0.0  # prometheus-fastapi-instrumentator 7.x requires starlette<1.0.0;
                           # PYSEC-2026-161 (Host header path injection, fix in 1.0.1) is
                           # blocked by this constraint. Mitigation: TrustedHostMiddleware
                           # validates Host before URL reconstruction in all production
                           # deployments (enforced by validate_api_runtime_settings).
                           # Re-evaluate when prometheus-fastapi-instrumentator releases
                           # starlette>=1.0.0 support.
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
orjson>=3.10
gunicorn>=23.0
prometheus-fastapi-instrumentator>=7.1
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9
alembic>=1.13
typer>=0.12
